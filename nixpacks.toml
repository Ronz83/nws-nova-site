[phases.setup]
nixPkgs = ["nodejs_23"]

[variables]
PORT = "3000"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx serve -s dist -l tcp://0.0.0.0:3000"
